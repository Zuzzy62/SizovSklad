<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sizovsklad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.JuniorWorkerActivity" />
        <activity android:name=".activities.SeniorWorkerActivity" />
        <activity android:name=".activities.ManagerActivity" />
        <activity android:name=".activities.DirectorActivity" />
        <activity android:name=".activities.ReceiptActivity" />
        <activity android:name=".activities.ConfirmOperationsActivity" />
        <activity android:name=".activities.ProductCatalogActivity" />
        <activity android:name=".activities.AddEditProductActivity" />
        <activity android:name=".activities.UserManagementActivity" />
    </application>
</manifest>